<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 按压时 -->
    <item android:drawable="@color/draw_text_color_p" android:state_pressed="true" />
    <!-- 默认时 -->
    <item android:drawable="@color/draw_text_color" />
</selector>